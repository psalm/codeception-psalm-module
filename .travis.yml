language: php
php:
    - 7.0
    - 7.1
    - 7.2
    - 7.3
    - 7.4snapshot
before_install:
    - phpenv config-rm xdebug.ini || true
install:
    - composer update

script: vendor/bin/codecept run -v
